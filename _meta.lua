local _ = require("gettext")
return {
    name = "askgemini",
    fullname = _("Ask Gemini"),
    description = _([[Sends highlighted text + book/chapter context to the Gemini API and shows the reply.]]),
}